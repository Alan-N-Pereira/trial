{
  "name": "speech2code.server",
  "version": "0.0.2-beta.1",
  "description": "",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/app.js",
    "build": "mkdir -p public && rm -rf public/* && cd ../webapp && npm run build && cp -R build/* ../server/public"
  },
  "keywords": [],
  "author": "Alan Pereira",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "path-browserify": "^1.0.1",
    "socket.io": "^3.1.1",
    "spoken": "file:../spoken/dist"
  }
}
